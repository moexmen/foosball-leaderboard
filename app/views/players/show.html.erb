<h1>Players</h1>

<h2>Profile</h2>
<p>
  <strong>Name:</strong>
  <%= @player.name %>
</p>
 
<p>
  <strong>Alias:</strong>
  <%= @player.alias %>
</p>

<p>
  <strong>Score</strong>
  <%= @player.name %>
  <strong>SCORE DETAILS</strong>
  <% @score_details.each do |key, value| %>
    <%= key %>: <%= value %><br>
  <% end %>
  <strong>MATCH DETAILS</strong>
  <% @match_details.each do |item| %>
      <% item.each do |key, value| %>
        <%=key %>: <%= value %><br>
      <% end %>
  <% end %>






</p>

<p>
  <strong>Matches:</strong>


</p>


 
<%= link_to 'Edit', edit_player_path(@player) %>
<%= link_to 'Delete', player_path(@player),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>
<%= link_to 'Back', players_path %>